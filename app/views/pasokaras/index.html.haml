= render "shared/search_field"
= render "shared/facet_tags"
= render "shared/sort_field"

= paginate @search

#pasokara-list
  - @pasokaras.each do |pasokara|
    - cache pasokara do
      = render "pasokaras/index_pasokara", pasokara: pasokara

= paginate @search
